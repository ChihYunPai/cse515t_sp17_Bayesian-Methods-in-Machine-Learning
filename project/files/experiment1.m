function []=experiment1()
% partition dataset into [train,test,val]=[.6,.2,.2]

% training for each individual patient in training set (in train_size) to produce train_size number of different GPR models

% Initialization of